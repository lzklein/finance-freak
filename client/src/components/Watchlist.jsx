import React from 'react'
import Asset from './Asset';

// full crud on assets
// contains asset cards, set buy/sell watch value (buy if higher than current value sell if lower)
const Watchlist = () => {
  return (
    <div>
      <Asset/>
    </div>
  )
}

export default Watchlist